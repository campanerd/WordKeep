info:
  name: Atualização de Decks
  type: http
  seq: 6

http:
  method: PUT
  url: "{{baseUrl}}/decks"
  body:
    type: json
    data: |-
      {
        "id": 14,
        "name": "Vocabulary"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
