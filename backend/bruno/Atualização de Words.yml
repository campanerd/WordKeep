info:
  name: Atualização de Words
  type: http
  seq: 5

http:
  method: PUT
  url: "{{baseUrl}}/words"
  body:
    type: json
    data: |-
      {
        "id": 43,
        "word": "whats up brother"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
