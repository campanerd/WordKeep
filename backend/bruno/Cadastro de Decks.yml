info:
  name: Cadastro de Decks
  type: http
  seq: 2

http:
  method: POST
  url: http://localhost:8080/decks
  body:
    type: json
    data: |-
      {
        "name":"teste12/05"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
