info:
  name: Cadastro de Words
  type: http
  seq: 3

http:
  method: POST
  url: "{{baseUrl}}/words"
  body:
    type: json
    data: |-
      {
          "word": "by",
          "deckId": 1
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
