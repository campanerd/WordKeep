info:
  name: Exclusão de Decks
  type: http
  seq: 9

http:
  method: DELETE
  url: "{{baseUrl}}/decks/{{idDeck}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
