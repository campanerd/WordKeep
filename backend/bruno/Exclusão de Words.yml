info:
  name: Exclusão de Words
  type: http
  seq: 8

http:
  method: DELETE
  url: "{{baseUrl}}/words/{{idWord}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
