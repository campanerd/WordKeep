info:
  name: Exclusão de Words
  type: http
  seq: 8

http:
  method: DELETE
  url: "{{baseUrl}}/words/1"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
