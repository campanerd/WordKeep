info:
  name: Listagem de Decks
  type: http
  seq: 7

http:
  method: GET
  url: "{{baseUrl}}/decks"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
