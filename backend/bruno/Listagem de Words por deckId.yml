info:
  name: Listagem de Words por deckId
  type: http
  seq: 4

http:
  method: GET
  url: "{{baseUrl}}/decks/14/words"
  auth: inherit

runtime:
  variables:
    - name: baseUrl
      value: ""

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
