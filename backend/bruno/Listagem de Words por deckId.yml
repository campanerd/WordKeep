info:
  name: Listagem de Words por deckId
  type: http
  seq: 4

http:
  method: GET
  url: "{{baseUrl}}/decks/{{idDeck}}/words"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
