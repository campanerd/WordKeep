info:
  name: Cadastro de Palavras
  type: http
  seq: 1

http:
  method: POST
  url: http://localhost:8080/words
  body:
    type: json
    data: |-
      {
        "word":"teste",
        "translation":"test",
        "sourceLanguage":"test1",
        "targetLanguage":"test2",
        "listaId":"123"
        
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
